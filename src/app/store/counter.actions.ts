import { createAction, props } from "@ngrx/store";

export const addFuncAction = createAction('increament');
export const subsFuncAction = createAction('substraction');
export const reset = createAction('reset');
