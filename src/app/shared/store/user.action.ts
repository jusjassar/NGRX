import { createAction } from "@ngrx/store";

export const AssignUser=createAction("Assign user");
export const ElevateUser=createAction('Elevate User');
export const DeleteUser=createAction('Delete User')